hour = input("Enter Hours: ")
rate = input("Enter Rate: ")
newHour = float(hour)
newRate = float(rate)
print(f"Pay:{newHour*newRate}")

# Convert to float
# hour = input("Enter Hours: ")
# rate = input("Enter Rate: ")
# hour = float(hour)
# rate = float(rate)
# pay = round(hour*rate,2)
# print(f"Pay: {pay}")